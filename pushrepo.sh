#!/bin/bash

git add *
git commit -m "Two_phases_flow.m had a problem with initial solution |x-xk| was small, lok where to stop deflation, residual is fine?? it depends on b, maybe this causes problems."
git remote add origin https://github.com/Gabydc/2018_February.git
git push -u origin master

